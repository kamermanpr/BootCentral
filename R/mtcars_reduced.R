#' @title Simplified version of the 'mtcars' dataset.
#'
#' @description This is a simplified version of the \emph{mtcars} dataset.
#'
#' @format The original dataset was extracted from the 1974 Motor Trend US magazine, and comprised fuel consumption and 10 aspects of automobile design and performance for 32 automobiles (1973–74 models). The reduced dataset consists of data from the 32 automobiles (rows), and 2 aspects of automobile design (columns):
#' \describe{
#'   \item{cyl}{Number of cylinders reported as a two-level categoricl variable: \emph{'v4'} (4 cylinders) and \emph{'v6+'} (6 or more cylinders).}
#'   \item{disp}{Engine displacement reported as two-level categoricl variable: \emph{'<100'} (displacement less than 100 cu.in.) and \emph{'>100'} (displacement greater than 100 cu.in.).}
#' }
#'
#' @source \code{\link[datasets]{mtcars}}

'mtcars_reduced'
